
general {
    # Hyprlock varsa otomatik çalışsın
    lock_cmd = pidof hyprlock || hyprlock

    # Uyku öncesi ekranı kilitle
    before_sleep_cmd = loginctl lock-session

    # Uykudan sonra scale bozulmasını düzelt
    # "sleep 2" NVIDIA'nın DRM devresini yeniden başlatması için bekleme süresi
    after_sleep_cmd = hyprctl reload && sleep 2 && hyprctl dispatch dpms on
}



listener {
    timeout = 120              # 5 dakika boşta kalınca
    on-timeout = brightnessctl -s set 10% # ekran kilidi
    0n-resume =  brightnessctl -r
}
lis
listener {
    timeout = 180              # 5 dakika boşta kalınca
    on-timeout = loginctl lock-session   # ekran kilidi
}
listener {
    timeout = 300           # 30 dakika boşta kalınca
    on-timeout = systemctl suspend     # sistemi uykuya al
}